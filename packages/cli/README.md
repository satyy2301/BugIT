# @bugit/cli

Plug-and-play BugIT CLI for local bug capture and replay.

## Install

```bash
npm install -g @bugit/cli
```

## Usage

```bash
cd your-project
bugit capture -- npm run dev
bugit replay
bugit doctor
```

See [local quickstart](../../docs/local-quickstart.md) for full walkthrough.
