Open the **BugIT** sidebar in the activity bar. No CLI install required — capture and replay binaries are bundled in the extension.

If **Record** flips back to Ready immediately, run **BugIT: Doctor** from the Command Palette (`Ctrl+Shift+P`) to check that the bundled CLI supports `record`.
